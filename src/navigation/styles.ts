import {StyleSheet} from 'react-native';

const useStyles = () => {
  return StyleSheet.create({
    container: {
      flexDirection: 'row',
    },
    logout: {
      marginLeft: 15,
    },
  });
};
export default useStyles;
